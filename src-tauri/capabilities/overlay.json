{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay",
  "description": "Capability for the fullscreen overlay overlay window",
  "windows": ["overlay"],
  "permissions": [
    "core:default",
    "log:default",
    "macos-permissions:default",
    "user-input:default",
    "core:window:default"
  ]
}
